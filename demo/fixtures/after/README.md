# Pricing lab

Módulo sem dependências para calcular totais monetários em centavos.

```bash
npm test
npm run check
```

O contrato público é `calculateTotalInCents(unitPriceInCents, quantity,
discountPercent = 0)`. O retorno é um inteiro em centavos.

